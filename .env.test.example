# Test Environment Variables for E2E Tests
# 测试环境变量配置
#
# IMPORTANT: Copy this file to .env.test and fill in actual values
# 重要：复制此文件为 .env.test 并填入实际值

# Generic test user (for backward compatibility)
# 通用测试用户（向后兼容）
TEST_EMAIL=your_test_email@example.com
TEST_PASSWORD=your_test_password

# Owner role test user (full permissions)
# Owner 角色测试用户（完全控制权限）
TEST_OWNER_EMAIL=your_owner_email@test.com
TEST_OWNER_PASSWORD=your_owner_password

# Editor role test user (can edit and comment)
# Editor 角色测试用户（可编辑和评论）
TEST_EDITOR_EMAIL=your_editor_email@test.com
TEST_EDITOR_PASSWORD=your_editor_password

# Viewer role test user (read-only access)
# Viewer 角色测试用户（只读访问）
TEST_VIEWER_EMAIL=your_viewer_email@test.com
TEST_VIEWER_PASSWORD=your_viewer_password

# Commenter role test user (can comment but not edit)
# Commenter 角色测试用户（可评论但不能编辑）
TEST_COMMENTER_EMAIL=your_commenter_email@test.com
TEST_COMMENTER_PASSWORD=your_commenter_password
